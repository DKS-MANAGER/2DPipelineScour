#!/bin/bash
set -e

. "$WM_PROJECT_DIR/bin/tools/RunFunctions"

echo "=== Resuming Phase 2 ==="
# Resume Phase 2 from latestTime (2.5s) and append to log.phase2
mpirun -np 6 sedExnerFoam -parallel >> log.phase2 2>&1

echo "=== Reconstructing results ==="
reconstructParMesh -latestTime > log.reconstructParMesh 2>&1
reconstructPar > log.reconstructPar 2>&1

rm -rf processor*
echo "=== Finished Resumed Phase 2 ==="
